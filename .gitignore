# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
dist/

# guide/index.html is intentionally committed — cloning the repo should be
# enough to read it, no build step required.
guide/index.pdf

# Test / tooling caches
.pytest_cache/
.ruff_cache/
coverage/

# Editors (no trailing slash: these are not always real directories)
.idea
.vscode

# Local agent config — personal, not part of the project. If this ever grows
# genuinely shared setup, .claude/settings.json is the file worth committing
# (force-add it); .claude/settings.local.json never is.
.claude

# OS
.DS_Store
